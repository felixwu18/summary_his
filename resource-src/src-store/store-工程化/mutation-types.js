/*
 包含所有mutation的type名称常量(状态对象state变化的内容,变化些what)
*/
export const INCREMENT = 'increment'   //增加的let_increment,这里右边有意写成小写的(写大写不影响)
export const DECREMENT = 'decrement' //减少的mutation
export const INCREMENTIFODD = 'incrementIfOdd' //
export const INCREMENTASYNC = 'incrementAsync' //
